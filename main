variable "name_prefix" {
  type        = string
  description = "Nom stable utilisé comme préfixe pour toutes les ressources de l'EC2 (par exemple: lch-cats-dev-services)"
}
name_prefix = "${var.entity}-${var.application}-${var.environment}-services"
name_prefix = "${var.entity}-${var.application}-${var.environment}-database"
