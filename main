-- Vérifier si LUCA est un utilisateur/schéma
SELECT username, default_tablespace, account_status 
FROM dba_users 
WHERE username = 'LUCA';
```

**Résultat attendu** :
```
USERNAME  DEFAULT_TABLESPACE  ACCOUNT_STATUS
LUCA      CATS_DATA          OPEN (ou LOCKED)

SELECT username, tablespace_name, max_bytes 
FROM dba_ts_quotas 
WHERE username = 'LUCA';

ALTER USER LUCA QUOTA UNLIMITED ON CATS_DATA;
