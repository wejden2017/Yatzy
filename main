resource "aws_api_gateway_vpc_link" "rest_vpc_link" {
  name        = "${var.entity}-${var.application}-${var.environment}-vpc-link"
  target_arns = [var.nlb_arn]  # 👈 passé en variable depuis ton module NLB

  tags = merge(
    var.tags,
    {
      Name = "${var.entity}-${var.application}-${var.environment}-vpc-link"
    }
  )
}
