-- Vérifier si LUCA est un utilisateur/schéma
SELECT username, default_tablespace, account_status 
FROM dba_users 
WHERE username = 'LUCA';
```

**Résultat attendu** :
```
USERNAME  DEFAULT_TABLESPACE  ACCOUNT_STATUS
LUCA      CATS_DATA          OPEN (ou LOCKED)

SELECT username, tablespace_name, max_bytes 
FROM dba_ts_quotas 
WHERE username = 'LUCA';

ALTER USER LUCA QUOTA UNLIMITED ON CATS_DATA;


-- Se connecter en SYSDBA
CONNECT / AS SYSDBA

-- Accorder directement à SEYDOU (bypass le rôle)
GRANT CREATE ANY TABLE TO SEYDOU;
GRANT CREATE ANY VIEW TO SEYDOU;
GRANT CREATE ANY PROCEDURE TO SEYDOU;
GRANT CREATE ANY SEQUENCE TO SEYDOU;
GRANT CREATE ANY TRIGGER TO SEYDOU;

GRANT DROP ANY TABLE TO SEYDOU;
GRANT DROP ANY VIEW TO SEYDOU;

COMMIT;

CREATE TABLE LUCA.EMPLOYES_TEST (
    id NUMBER PRIMARY KEY,
    nom VARCHAR2(100) NOT NULL,
    prenom VARCHAR2(100) NOT NULL,
    email VARCHAR2(200),
    date_embauche DATE DEFAULT SYSDATE,
    salaire NUMBER(10,2)
);

SELECT owner, trigger_name, trigger_type, triggering_event, status
FROM dba_triggers
WHERE base_object_type = 'SCHEMA'
  AND triggering_event LIKE '%DDL%'
ORDER BY owner, trigger_name;

SELECT * FROM dba_policies;

-- Vérifier Oracle Label Security
SELECT * FROM dba_sa_users WHERE user_name = 'SEYDOU';
