BigDecimal montantTotal = report.getAccountBalances() == null || report.getAccountBalances().isEmpty()
        ? BigDecimal.ZERO
        : report.getAccountBalances()
            .stream()
            .map(a -> a.getMontantValo())
            .reduce(BigDecimal.ZERO, BigDecimal::add);
