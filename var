cd ~/cats-db-user-admin/

export SYSDBA_PASSWORD="ton_mot_de_passe"

# Test connexion
ansible -i inventory/local_dev.yml oracle_servers -m ping

# Lancer le playbook
ansible-playbook \
  -i inventory/local_dev.yml \
  playbooks/manage_db_users.yml \
  -e ENV=dev \
  --tags role \
  -vv
