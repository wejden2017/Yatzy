
private isIUA(hostname: string, pathname: string) : boolean {
  // https://cats-iua.corp.lch.com/cats
  // http://172.18.128.52:8080/cats
  // http://10.63.76.29:8080/cats
  // http://172.18.128.65:8080/cats (luca7)
  // http://10.63.72.228:8080/cats (luca7 vu de windows10)
  
  console.log('  🔸 isIUA - hostname:', hostname);
  console.log('  🔸 isIUA - pathname:', pathname);
  console.log('  🔸 EnvService.IUA:', EnvService.IUA);
  console.log('  🔸 EnvService.CATS:', EnvService.CATS);
  console.log('  🔸 IUA.test(hostname):', EnvService.IUA.test(hostname));
  console.log('  🔸 CATS.test(pathname):', EnvService.CATS.test(pathname));
  console.log('  🔸 DEV1.test(hostname):', EnvService.DEV1.test(hostname));
  console.log('  🔸 DEV2.test(hostname):', EnvService.DEV2.test(hostname));
  console.log('  🔸 DEV3.test(hostname):', EnvService.DEV3.test(hostname));
  console.log('  🔸 DEV4.test(hostname):', EnvService.DEV4.test(hostname));
  
  const result = (
    (EnvService.IUA.test(hostname) && EnvService.CATS.test(pathname))
    || EnvService.DEV1.test(hostname)
    || EnvService.DEV2.test(hostname)
    || EnvService.DEV3.test(hostname)
    || EnvService.DEV4.test(hostname));
  
  console.log('  🔸 Final isIUA result:', result);
  
  return result;
}
