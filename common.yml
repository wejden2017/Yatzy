InterruptedException n’est plus gérée alors que watchService.take() peut la lever. Soit il faut rétablir un catch (InterruptedException), soit déclarer throws InterruptedException sur watchDirectory(). En l’état c’est bloquant.
