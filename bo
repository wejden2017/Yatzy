# DEBUG: Output pour voir ce qui est trouvé
output "debug_protection" {
  value = {
    instances_with_protected_tag = data.aws_instances.protected.ids
    instances_count = length(data.aws_instances.protected.ids)
    private_ips = data.aws_instances.protected.private_ips
  }
}
