- name: permissions .profile VTOM
  file:
    path: "{{ item }}"
    owner: vtom
    group: vtom
  become: true
  loop:
    - "{{ vtom_install_dir }}/.profile"
    - "{{ vtom_install_dir }}/.bashrc"
    - "{{ vtom_install_dir }}/.kshrc"
  when: do_install | bool
  ignore_errors: true  
