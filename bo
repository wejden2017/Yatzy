- name: Install required packages via raw
  ansible.builtin.raw: sudo yum install -y ksh unzip expect libselinux-python python3-pexpect
  register: install_packages
  changed_when: "'Complete' in install_packages.stdout"
  become: true
