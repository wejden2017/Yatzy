- name: Ensure local artifacts folder exists (shell fallback)
  ansible.builtin.shell: 'mkdir -p "{{ lookup(''env'',''CI_PROJECT_DIR'') }}/artifacts"'
  args:
    executable: /bin/sh
  delegate_to: localhost
  run_once: true
  changed_when: false
  tags: [update_database]
