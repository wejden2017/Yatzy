0-terraform-clean:
  stage: Validation
  script:
    - echo "🧹 Nettoyage .terraform & fichiers obsolètes"
    - rm -rf .terraform terraform.tfstate terraform.tfstate.backup
    - find . -name "*.tfplan" -exec rm -f {} \;
  tags:
    - default
