- name: Extraire les versions Tomcat de la page
  set_fact:
    tomcat_versions: >-
      {{ tomcat_page.content 
      | regex_findall('(\d+\.\d+\.\d+)') 
      | list }}
- name: Afficher les versions trouvées
  debug:
    msg: "Versions trouvées : {{ tomcat_versions }}"

